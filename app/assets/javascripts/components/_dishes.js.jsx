const styles = {
    margin: "1em",
    padding: "1em",
    border: "1px solid #ddd"
  };

  const Dishes = props => {
    return (
      <div style={styles}>
        
        <DishesContainer />
      </div>
    );
  };